// String, number, undefined, null, boolean, symbol.
const nome = "Jonas"; // String = Pode usar aspas simples, duplas ou crases
const num1 = 10; // number
const num2 = 10.52; // number tmb, porem de ponto flutuante
let nomeAluno; //  undefined = nao aponta pra local algum na memoria
const sobrenomeAluno = null; // Nulo = nao aponta pra local algum na memoria tambem, porem estou explicitamente querendo que esta variavel tenha este valor.
const boolean = true; //somente pode ter valor true ou false (valores logicos)
